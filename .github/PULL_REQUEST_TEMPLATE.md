## What changed

<!-- One line. If this is a data change, name the model(s). -->

## Data checklist

<!-- Delete this section for code-only changes. -->

- [ ] Every date I added or changed is stated at a URL listed in `sources`
- [ ] I moved `last_verified` forward on every entry whose dates I touched
- [ ] I used `shutdown_on` for a committed date and `earliest_shutdown_on` for a
      provider estimate ("not sooner than", "earliest possible")
- [ ] Every `replacements` entry points at a page that exists, or is marked
      `external: true`
- [ ] `npm run validate` passes

## Notes

<!-- Anything a reviewer should check, especially a date that was hard to
     confirm or a provider page that has since changed. -->
